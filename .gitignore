# vscode
.vscode 

# Intellij
*.iml
.idea

# npm
node_modules
.vectra-index
.server.json
dist

# Don't include the compiled main.js file in the repo.
# They should be uploaded to GitHub releases instead.
main.js
.env

# Exclude sourcemaps
*.map

# dev-only tooling: kept locally, never pushed to the remote
src/dev/

# obsidian
data.json
embeddings.bin
# ...but keep committed migration test fixtures (frozen golden vaults)
!test/fixtures/**/data.json

# Exclude macOS Finder (System Explorer) View States
.DS_Store
